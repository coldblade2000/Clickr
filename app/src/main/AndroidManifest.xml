<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.twotowerstudios.clickr">

    <application
        android:allowBackup="true"
        android:configChanges="orientation"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <uses-feature
            android:name="android.hardware.touchscreen.multitouch"
            android:required="true" />

        <activity
            android:name=".MainMenu"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DifficultyScreen"
            android:label="@string/title_activity_difficulty_screen" />
        <activity
            android:name=".InGameClassic"
            android:label="@string/title_activity_in_game_classic"></activity>
        <activity
            android:name=".PostGameScreen"
            android:label="@string/title_activity_post_game_screen"></activity>
    </application>

</manifest>
